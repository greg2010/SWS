{% extends "baseNoAuth.twig" %}

{% block title %}Registration{% endblock %}

{% block content %}
    {% if errorMsg is defined %}
        {% set isErrorHidden = "" %}
    {% else %}
        {% set isErrorHidden = "hidden" %}
    {% endif %}

  {#{% if fromPost == '1' %} {# Проверка что юзер действительно пришел из формы. #}
    {#{% if success == '1' %} {# Проверка на успешность логина #}
        {# Вывод сообщения об успешной регистрации и редиректим (куда?) #}
    {#{% elseif %}
        {# Вывод сообщения о неудачной попытке регистрации (на стороне сервера). #}
    {#{% endif %}
{#{% elseif %} #}
    
 <form action="login.php" method="post">
   <h1>LOG IN</h1>
   <div class="inset">
          <div class="alert alert-danger" role="alert" {{isErrorHidden}}>
              {{errorMsg}}
          </div>
      <p>
        <label for="email">INGAME NAME</label>
        <input type="text" name="login" value ="{{saveform.login}}" id="email">
      </p>
      <p>
        <label for="password">PASSWORD</label>
        <input type="password" name="password" id="password" style="color: #f6c000;">
      </p>
  <hr />
    <div class="amPmCheckbox">
        <input type="checkbox" name="remember" class="checkboxLabel main_street_input" id="ch1" value="1" checked />
        <label for="ch1">Remember me</label>
    </div>
  </div>
      <p class="p-container">
        <span><a href="#">Forgot password ?</a></span>
        <input type="hidden" name="form" value="sent">
        <input type="submit" name="go" id="go" value="Log in">
      </p>
</form>
    
     
{% endblock %}